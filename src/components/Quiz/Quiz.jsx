import React from "react";
import { Container, Typography } from "@mui/material";

const Quiz = () => {
	return (
		<Container>
			<Typography variant="h3">Quiz Page</Typography>
			<Typography variant="body1">
				Welcome to the quiz! Here you can test your knowledge.
			</Typography>
			{/* needs logicx*/}
		</Container>
	);
};

export default Quiz;